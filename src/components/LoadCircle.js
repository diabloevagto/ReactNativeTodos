import React from 'react'
import {
  ActivityIndicator,
  StyleSheet,
  View,
} from 'react-native'

const LoadCircle = () => (
  <View>
    <ActivityIndicator size="large" color="#0000ff" />
  </View>
)

export default LoadCircle
